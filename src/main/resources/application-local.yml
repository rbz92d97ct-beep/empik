spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/coupons
    username: coupons
    password: coupons
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

logging:
  level:
    com.empik: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
